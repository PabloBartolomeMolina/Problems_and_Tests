#include <fstream>  
#include <iostream>  
#include <regex>
#include <string>

using namespace std;

using std::string;
using std::getline;
using std::regex;

bool checkFile(string input);
int countWords(string path);